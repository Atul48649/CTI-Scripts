const {
    BASE_URL
} = require('./general');

const {
    STUDENTDASHBOARD
} = require('./general');

exports.studentDashboardScreen = {
    studentDashboardUrl : `${BASE_URL}${STUDENTDASHBOARD}`,
}